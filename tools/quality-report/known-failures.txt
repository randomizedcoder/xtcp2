# Pre-existing test failures the quality-report should not count as new
# regressions. Format: one entry per line, either
#   <Package>.<Test>          (full path)
# or
#   <Test>                    (any test of that name, in any package)
#
# Remove an entry once the test is fixed.

# (no known-failure entries — TestCheckFilePermissions was fixed to use
# t.TempDir() fixture files instead of hardcoded /bin/bash + /bin/ls)
